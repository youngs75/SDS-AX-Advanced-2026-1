# 세션 인수인계 — 2026-04-03-0001

## 프로젝트 개요
SDS AX Advanced 2026-1 교육과정. 최종 산출물: AI Assistant Coding Agent Harness.
(참고: "Prime Jennie"는 별도 개인 프로젝트이며, 교육과정 산출물과 무관)

## 최근 작업 내역

### 1. 강사 리포 동기화
- `origin`(HyunjunJeon)에서 Day04, Day05 커밋 fetch + merge
- Day-03 노트북 충돌 2건 → 강사 버전 기준으로 해결
- `mine`(youngs75)에 push 완료
- upstream을 `mine/main`으로 변경 (VS Code 표시 정상화)

### 2. youngs75_a2a 루트 레벨 이동
- `Day-04/youngs75_a2a/` → 프로젝트 루트 `youngs75_a2a/`로 이동
- Dockerfile, docker-compose, 테스트, AGENTS.md 내 경로 참조 일괄 수정
- 교육 기간 전체에서 지속 개선할 수 있도록 루트 배치

### 3. Orchestrator 에이전트 추가
- `youngs75_a2a/agents/orchestrator/` 신규 구현
  - `config.py` — OrchestratorConfig + AgentEndpoint (에이전트 목록 관리)
  - `schemas.py` — OrchestratorState (selected_agent, agent_response)
  - `agent.py` — OrchestratorAgent (CLASSIFY → DELEGATE → RESPOND)
- LLM 기반 의도 분류로 하위 에이전트 자동 라우팅
- A2A 프로토콜로 하위 에이전트에 요청 위임
- 라우팅 검증 완료: 뉴스→simple-react, 심층분석→deep-research, 보고서→deep-research-a2a
- 서버: `python -m youngs75_a2a.tests.run_orchestrator_server` (포트 18080)

### 4. Langfuse 로컬 환경 구축
- `Day-05/langfuse-docker-compose.yml` 수정
  - langfuse-web 서비스 추가 (포트 3100)
  - **langfuse-worker** 서비스 추가 (OTEL → ClickHouse 처리 핵심)
  - ClickHouse 네이티브 포트 9000→9005 변경 (충돌 회피)
  - CLICKHOUSE_CLUSTER_ENABLED=false 설정 (단일 노드)
- 6개 컨테이너: Postgres, ClickHouse, Redis, MinIO, langfuse-web, langfuse-worker
- Langfuse v3.163.0 정상 실행 확인

### 5. Deep Agents + Langfuse 통합 예제
- `Day-05/deepagent_langfuse.py` 작성
  - create_deep_agent + calculate/get_current_date 도구
  - Langfuse CallbackHandler로 트레이스 자동 수집
  - 3개 질문 테스트 → Langfuse UI에서 트레이스 확인 완료
- langfuse 4.0.6 패키지 설치 (pyproject.toml 반영)

### 6. 메모리 업데이트
- "Prime Jennie"는 개인 프로젝트, 교육과정 산출물은 "AI Assistant Coding Agent Harness"로 명확히 구분

## 현재 상태
- 커밋 `08f1049` — mine/main에 push 완료 (Orchestrator + 루트 이동)
- Langfuse 6개 컨테이너 실행 중
- 미커밋 변경: langfuse-docker-compose.yml, deepagent_langfuse.py, pyproject.toml, uv.lock

## 다음 단계

### 단기
- [ ] 스트리밍 응답 UX 개선 — `send_message_streaming()` 적용
- [ ] A2AClient deprecation 해결 — `ClientFactory.connect()` 마이그레이션
- [ ] Orchestrator E2E 테스트 (Docker 환경에서 전체 파이프라인)

### 중기 (Day-07~09)
- [ ] Coding Agent 구현 시작 — CODING_AGENT_DESIGN.md P1~P7 적용
  - P1: 최소 3노드 (parse → execute → verify)
  - P2: generation/verification 모델 분리
  - P3: core/action_validator.py Safety Envelope
- [ ] agents/coding_assistant/ 추가

### 최종 (Day-10)
- [ ] AI Assistant Coding Agent Harness 최종 산출물 완성

## 중요 참고사항

### 환경 설정
- `.env` 파일: `Day-05/.env` (Langfuse 키 포함), `youngs75_a2a/.env` (A2A용)
- Git remote: `origin` = 강사(HyunjunJeon), `mine` = youngs75 (upstream)
- Python: 3.13, uv 기반 패키지 관리

### Langfuse 운영 명령어
```bash
cd Day-05
docker compose -f langfuse-docker-compose.yml up -d    # 기동
docker compose -f langfuse-docker-compose.yml ps        # 상태
docker compose -f langfuse-docker-compose.yml down       # 종료
# UI: http://localhost:3100
```

### 해결된 주요 이슈
- Langfuse worker 누락 → langfuse-worker:3 서비스 추가로 OTEL→ClickHouse 파이프라인 정상화
- ClickHouse Zookeeper 오류 → CLICKHOUSE_CLUSTER_ENABLED=false로 해결
- 포트 충돌 → ClickHouse 9000→9005, Langfuse 3000→3100
- NEXTAUTH_URL 리디렉션 → 3100 포트로 설정
- Docker Hub rate limit → docker login으로 해결
