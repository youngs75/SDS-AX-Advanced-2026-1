# 세션 인수인계 — 2026-04-02-0001

## 프로젝트 개요
SDS AX Advanced 2026-1 교육과정 Day-04 실습.
참조 구현(`Day-04/a2a/`)을 분석하고 `youngs75_a2a` 프레임워크를 처음부터 설계·구현하여 프로덕션 배포까지 완료.

## 최근 작업 내역

### 1. a2a/ 참조 구현 분석
- 폴더 구조, AgentExecutor 패턴, LangGraph 취소 메커니즘 심층 분석
- 참조 구현 8,282줄의 아키텍처와 한계점 파악

### 2. youngs75_a2a 프레임워크 구현 (2,174줄)
- **core/** — BaseGraphAgent, BaseAgentConfig(모델 팩토리), MCPToolLoader(인스턴스 기반), tool_call_utils(중복 제거), override_reducer
- **a2a/** — BaseAgentExecutor(범용) + LGAgentExecutor(LangGraph) + Server 조립
- **agents/** — 3종 에이전트:
  - SimpleReAct: MCP 도구 활용 ReAct
  - DeepResearch: clarify → brief → supervisor(병렬 연구) → report
  - DeepResearchA2A: A2A supervisor 위임 + HITL interrupt 승인 루프

### 3. Docker 프로덕션 배포
- MCP 3개 (Tavily:3001, arXiv:3000, Serper:3002) + Agent 3개 (SimpleReAct:18081, DeepResearch:18082, DeepResearchA2A:18083) = 6컨테이너
- docker-compose.yml, Dockerfile.mcp, Dockerfile.agent 작성
- 헬스체크, depends_on, Docker 네트워크 구성 완료

### 4. 테스트
- Step 1 (프레임워크 자체) — 통과
- Step 2 (LLM 연동) — 통과
- Step 3 (전체 파이프라인) — 통과
- Docker E2E — 6/6 서비스 정상, 3개 Agent 모두 A2A 프로토콜 응답 확인
  - SimpleReAct: 452자, DeepResearch: 9,233자, DeepResearchA2A: 6,144자
- 대화형 클라이언트(demo_client.py) 구현 및 실시간 에이전트 전환 지원

### 5. 문서
- REPORT.md — 개발완료 보고서 (참조 대비 74% 코드 감소, 기술 스택, 설계 패턴)
- CODING_AGENT_DESIGN.md — 논문 7편 인사이트 기반 Coding Agent 설계 가이드 (P1~P7 체크리스트)
- AGENTS.md — 프로젝트 규칙 문서 (세션 관리, 커밋 규칙 등)

### 6. 강사 리포 동기화 및 Push
- origin(HyunjunJeon) fetch + merge 완료 (Day04 오전수업, 법 문서 마크다운)
- mine(youngs75)에 push 완료

## 현재 상태
- Docker 6개 컨테이너 실행 중 (`cd Day-04/youngs75_a2a/docker && docker compose ps`)
- 모든 테스트 통과
- 커밋 `6d678e5` — mine/main에 push 완료

## 다음 단계

### 단기 (Day-05~06)
- [ ] 스트리밍 응답 UX 개선 — `send_message_streaming()` 적용하여 중간 결과 실시간 출력
- [ ] A2AClient deprecation 해결 — `ClientFactory.connect()` 방식으로 마이그레이션
- [ ] 2순위 대기 논문 분석 (ATOM, 뉴로심볼릭AI 등)

### 중기 (Day-07~09)
- [ ] Coding Agent 구현 시작 — CODING_AGENT_DESIGN.md P1~P7 순서 적용
  - P1: 최소 3노드 (parse → execute → verify)
  - P2: generation/verification 모델 분리
  - P3: core/action_validator.py Safety Envelope
- [ ] agents/coding_assistant/ 추가

### 최종 (Day-10)
- [ ] AI Assistant Coding Agent Harness ("Prime Jennie") 최종 산출물 완성
- [ ] 졸업 lifecycle 프로세스 설계

## 중요 참고사항

### 환경 설정
- `.env` 파일: `Day-04/youngs75_a2a/.env` (API 키 포함, 커밋 금지)
- Git remote: `origin` = 강사, `mine` = youngs75
- Python: 3.13, uv 기반 패키지 관리

### 메모리 시스템
- `~/.claude/projects/.../memory/` 에 논문 인사이트, 사용자 프로필 저장됨
  - `project_paper_insights.md` — 7편 논문 → my_a2a 아키텍처 매핑
  - `user_profile.md` — 사용자 정보

### 해결된 주요 이슈
- MCP 서버 Host 헤더 검증 → FastMCP에 `host="0.0.0.0"` 설정으로 해결
- LGAgentExecutor 텍스트 추출 → astream 청크가 `{노드명: {필드: 값}}` 구조, 매 청크 누적 추출로 해결
- Docker 헬스체크 → MCP `/mcp` 엔드포인트가 406 반환, `curl -so /dev/null` 방식으로 해결
- auto_build 중복 전달 → `kwargs.pop("auto_build", None)` 으로 해결

### Docker 운영 명령어
```bash
cd Day-04/youngs75_a2a/docker
docker compose up -d          # 기동
docker compose ps             # 상태
docker compose logs -f        # 로그
docker compose down           # 종료

# E2E 테스트
cd Day-04 && export $(grep -v '^#' youngs75_a2a/.env | xargs)
python -m youngs75_a2a.tests.test_docker_e2e

# 대화형 클라이언트
python -m youngs75_a2a.tests.demo_client --agent deep
```
